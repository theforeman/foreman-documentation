:_mod-docs-content-type: PROCEDURE

[id="installing-{smart-proxy-context}-server-packages"]
= Installing {SmartProxyServer} packages

[role="_abstract"]
Install {SmartProxyServer} packages to set up the core software components needed for {SmartProxy} functionality.

.Procedure
. Upgrade all packages:
+
[options="nowrap" subs="+quotes,attributes"]
----
# {package-upgrade}
----
. Install the packages:
+
[options="nowrap" subs="+quotes,attributes"]
----
# {package-install} {smartproxy-installer-package}
----
