:_mod-docs-content-type: PROCEDURE

[id="configuring-selinux-to-permit-content-synchronization-on-custom-ports_{context}"]
= Configuring SELinux to permit content synchronization on custom ports

[role="_abstract"]
Add a TCP port to the SELinux `http_port_t` type when upstream content servers use a port that {Project} cannot reach by default for synchronization.

By default, connecting to web servers running on the following ports is permitted: 80, 81, 443, 488, 8008, 8009, 8443, and 9000.

.Procedure
. On {ProjectServer}, verify the ports that are permitted by SELinux for content synchronization:
+
[options="nowrap",subs="+quotes"]
----
# semanage port -l | grep ^http_port_t
http_port_t     tcp      80, 81, 443, 488, 8008, 8009, 8443, 9000
----
. Configure SELinux to permit a custom port for content synchronization, for example 10011:
+
[options="nowrap",subs="+quotes"]
----
# semanage port -a -t http_port_t -p tcp 10011
----
