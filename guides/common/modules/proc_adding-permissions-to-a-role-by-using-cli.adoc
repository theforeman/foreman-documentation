:_mod-docs-content-type: PROCEDURE

[id="adding-permissions-to-a-role-by-using-cli"]
= Adding permissions to a role by using Hammer CLI

[role="_abstract"]
Use this procedure to add permissions to a role by using Hammer CLI.

.Procedure
. List all available permissions:
+
[options="nowrap", subs="+quotes,attributes"]
----
$ hammer filter available-permissions
----
. Add permissions to a role:
+
[options="nowrap", subs="+quotes,attributes"]
----
$ hammer filter create \
--permission-ids _My_Permission_ID_1_,_My_Permission_ID_2_ \
--role _My_Role_Name_
----
+
For more information about roles and permissions parameters, enter the `hammer role --help` and `hammer filter --help` commands.
