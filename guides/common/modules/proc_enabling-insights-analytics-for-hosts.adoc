:_mod-docs-content-type: PROCEDURE

[id="enabling-{insights-id}-analytics-for-hosts_{context}"]
= Enabling {Insights} analytics for hosts

[role="_abstract"]
You can enable the Insights client on hosts and have {Project} upload host inventory to the {Insights} service in the {RHCloud}.

ifdef::katello,foreman-el,foreman-deb,orcharhino[]
{Insights} is a service by Red Hat for {RHEL} hosts.
Ensure to set these parameters for {RHEL} hosts only.
If you set the parameter on any non-{RHEL} operating systems, {Project} automatically uploads new reports to the {RHCloud} when enabled in RH Cloud {Project} settings.
endif::[]

You can set these parameters at any level.
For more information, see xref:Host_Parameter_Hierarchy_managing-hosts[].

.Procedure
. In the {ProjectWebUI}, navigate to *Host* > *Provisioning Setup* > *Operating Systems*.
. Select any {RHEL} operating systems for which you want to change the value.
. Enable installation of the {Insights} client on hosts during host registration:
.. On the *Parameters* tab, add the `host_registration_insights` parameter, select the *boolean* type, and set the value to *True*.
.. Click *Submit* to save the parameter.
ifdef::insights-in-cloud[]
. Enable host inventory upload to the {RHCloud} during host registration:
.. On the *Parameters* tab, add the `host_registration_insights_inventory` parameter, select the *boolean* type, and set the value to *True*.
.. Click *Submit* to save the parameter.
endif::[]
