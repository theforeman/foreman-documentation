:_mod-docs-content-type: PROCEDURE

[id="resetting-custom-ssl-certificate-to-default-self-signed-certificate-on-{project-context}-server"]
= Resetting custom SSL certificate to default self-signed certificate on {ProjectServer}

[role="_abstract"]
If you want to revert to the default configuration, you can reset a custom SSL certificate to the default self-signed certificate on your {ProjectServer}.

.Procedure
* Reset the custom SSL certificate to default self-signed certificate:
+
[options="nowrap" subs="+quotes,attributes"]
ifdef::containerized[]
----
# {foremanctl} deploy --certificate-source default
----
endif::[]
ifndef::containerized[]
----
# {foreman-installer} --certs-reset
----
endif::[]

.Verification
* In your browser, go to your {ProjectServer} login page, for example, `\https://{foreman-example-com}`, and inspect the certificate in the browser.
This is typically displayed as a shield, padlock, or tune icon next to the address bar depending on your browser.
* On the command line, display the certificate:
+
[options="nowrap" subs="+quotes,attributes"]
----
$ openssl s_client -connect {foreman-example-com}:443 </dev/null
----

.Additional resources
* {InstallingSmartProxyDocURL}resetting-custom-ssl-certificate-to-default-self-signed-certificate-on-{smart-proxy-context}-server[Resetting custom SSL certificate to default self-signed certificate on {SmartProxyServer} in _{InstallingSmartProxyDocTitle}_]
* {ManagingHostsDocURL}resetting-custom-ssl-certificate-to-default-self-signed-certificate-on-hosts_managing-hosts[Resetting custom SSL certificate to default self-signed certificate on hosts in _{ManagingHostsDocTitle}_]
