:_mod-docs-content-type: PROCEDURE

[id="build-an-ansible-execution-environment-by-using-api"]
= Build an Ansible Execution Environment by using {Project} API

[role="_abstract"]
You can build an Ansible Execution Environment in {Project} by using {Project} API.
An Ansible Execution Environment is defined by its base container image and version of `ansible-core`.

.Prerequisites
* Your {Project} account has a role that grants the `create_ansible_execution_environments`, `edit_ansible_execution_environments`, `view_ansible_content`, `view_ansible_execution_environments`, and `view_organizations` permissions.

.Procedure
. Create a JSON file, for example, `data.json` to define your Ansible Execution Environment:
+
[source, json, options="nowrap", subs="verbatim,quotes,attributes"]
----
{
  "organization_id": "_My_Organization_ID_",
  "execution_environment": {
    "name": "_My_Ansible_Execution_Environment_Name_",
    "base_image_url": "_My_Base_Container_Image_URL_",
    "ansible_version": "_My_Ansible_Version_"
  }
}
----
. Build your Ansible Execution Environment:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--data @data.json \
--header "Accept: application/json" \
--header "Content-Type: application/json" \
--request POST \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/execution_environments/ \
| jq
----

.Verification
* Query the {Project} API to verify that {Project} built the Ansible Execution Environment successfully:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/execution_environments/ \
| jq ".results | .[]"
----
+
Verify that `build_status` reported by {Project} equals `success`.

.Additional resources
* https://pypi.org/project/ansible-core/#history[Release history of `ansible-core`]
* {ContentManagementDocURL}importing-container-images[Importing container images]
