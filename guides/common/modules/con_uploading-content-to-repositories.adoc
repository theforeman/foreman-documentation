:_mod-docs-content-type: CONCEPT

[id="uploading-content-to-{customreposid}"]
= Uploading content to {customrepos}

[role="_abstract"]
Upload packages directly into {customrepos} when no trustworthy upstream URL exists so you can still distribute internally built or licensed artifacts to managed hosts.
