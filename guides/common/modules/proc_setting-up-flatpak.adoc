[id="setting-up-flatpak"]
= Setting up Flatpak

This procedure installs the Flatpak package manager.

.Procedure
* Install the `flatpak` package:
+
[subs="+attributes"]
----
# {client-content-type} install flatpak
----
