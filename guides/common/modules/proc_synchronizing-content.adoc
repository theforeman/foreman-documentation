:_mod-docs-content-type: PROCEDURE

[id="synchronizing-content_{context}"]
= Synchronize your content

[role="_abstract"]
Synchronization downloads metadata and packages from an upstream source into {Project}.
Until you synchronize a repository, {Project} has no content to distribute to your hosts.
This initial synchronization can take a while depending on the size of the repository and your network speed.

.Prerequisites
* You have at least one product with a repository.
ifdef::satellite[]
For Red{nbsp}Hat content, ensure that you have imported a subscription manifest and enabled the repositories you need.
endif::[]

.Procedure
. In the {ProjectWebUI}, navigate to *Content* > *Products*.
. Select the product that contains the repositories you want to synchronize.
. Select the checkboxes next to the repositories you want to synchronize.
. Click *Sync Now*.
+
{Project} creates a synchronization task.
Depending on the repository size, the sync can take from a few minutes to over an hour.

.Verification
. In the {ProjectWebUI}, navigate to *Content* > *Sync Status*.
. Expand the product to see the synchronization result for each repository.
. Verify that the synchronization completed successfully and that the repository shows a package count.

After the sync finishes, your content is stored in the _Library_ environment.
Library is the default lifecycle environment where all content lands first.
In the next step, you create additional lifecycle environments to stage content through a promotion path.

.Next steps
* xref:defining-lifecycle-environments_{context}[]

.Additional resources
* {ContentManagementDocURL}synchronizing-repositories-by-using-web-ui[Synchronizing repositories by using {ProjectWebUI}] in _{ContentManagementDocTitle}_
