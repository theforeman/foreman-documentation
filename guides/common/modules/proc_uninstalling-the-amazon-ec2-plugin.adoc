:_mod-docs-content-type: PROCEDURE

[id="uninstalling-the-amazon-ec2-plugin"]
= Uninstalling the Amazon EC2 plugin

[role="_abstract"]
If you have previously installed the Amazon EC2 plugin but do not use it anymore to manage and deploy hosts to EC2, you can uninstall it from your {ProjectServer}.

.Procedure
. Remove the Amazon EC2 package from your {ProjectServer}:
+
[options="nowrap", subs="+quotes,verbatim,attributes"]
----
# {project-package-remove} foreman-ec2
----
. Disable the Amazon EC2 compute resource:
+
[options="nowrap", subs="+quotes,verbatim,attributes"]
----
# {foreman-installer} --no-enable-foreman-compute-ec2
----

.Verification
. In the {ProjectWebUI}, navigate to *Administer* > *About*.
. Select the *Available Providers* tab to verify the removal of the Amazon EC2 plugin.
