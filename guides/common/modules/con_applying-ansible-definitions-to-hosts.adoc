:_mod-docs-content-type: CONCEPT

[id="applying-ansible-definitions-to-hosts"]
= Applying Ansible definitions to hosts

[role="_abstract"]
When a host has an Ansible role assigned to it, {Project} can run the role on the host to apply and enforce the configuration state that the role defines.
