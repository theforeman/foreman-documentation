:_mod-docs-content-type: CONCEPT

[id="ssl-authentication-overview"]
= SSL authentication overview

[role="_abstract"]
{ProjectName} requires HTTPS for all API communications to provide encryption and identity verification.
You can use either self-signed certificates or custom SSL certificates you configure.

{Project} {ProjectVersion} does not support non-SSL communications.

By default, {ProjectServer} uses a self-signed certificate.
This certificate acts as both the server certificate to verify the encryption key and the certificate authority (CA) to trust the identity of {ProjectServer}.

.Additional resources
* {InstallingServerDocURL}configuring-{project-context}-server-with-a-custom-ssl-certificate[Configuring {ProjectServer} with a custom SSL certificate in _{InstallingServerDocTitle}_]
ifdef::satellite[]
* {InstallingServerDisconnectedDocURL}configuring-{project-context}-server-with-a-custom-ssl-certificate[Configuring {ProjectServer} with a custom SSL certificate in _{InstallingServerDisconnectedDocTitle}_]
endif::[]
