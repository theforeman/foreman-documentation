:_mod-docs-content-type: CONCEPT

[id="{smart-proxy-context}-server-registration"]
= {SmartProxyServer} registration

[role="_abstract"]
Registering the base operating system to {ProjectServer} establishes the connection needed to access synchronized repositories and download {SmartProxyServer} installation packages.
