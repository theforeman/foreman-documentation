:_mod-docs-content-type: CONCEPT

[id="apply-ansible-definitions-to-hosts"]
= Apply Ansible definitions to hosts

[role="_abstract"]
When a host has one or more Ansible roles assigned to it, {Project} can run the roles on the host to apply and enforce the configuration state that the roles define.
