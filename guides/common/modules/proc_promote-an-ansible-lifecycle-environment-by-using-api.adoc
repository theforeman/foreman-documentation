:_mod-docs-content-type: PROCEDURE

[id="promote-an-ansible-lifecycle-environment-by-using-api"]
= Promote an Ansible lifecycle environment by using {Project} API

[role="_abstract"]
You can promote an Ansible lifecycle environment within an Ansible lifecycle environment path by using {Project} API.
Ansible lifecycle environments are named sets of Ansible content and Ansible Execution Environments.

.Prerequisites
* Your {Project} account has a role that grants the `create_ansible_lifecycle_environment_paths`, `edit_ansible_lifecycle_environment_paths`, `view_ansible_content`, `view_ansible_lifecycle_environment_paths`, `view_ansible_lifecycle_environments`, and `view_organizations` permissions.
* You have at least two Ansible lifecycle environments within an Ansible lifecycle environment path.
* You have assigned Ansible content, an Ansible Execution Environment, or both, to your left Ansible lifecycle environment.

.Procedure
// TODO: Fix API endpoint
. Optional: Get a list of all Ansible lifecycle environments with your Ansible lifecycle environment path:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/lifecycle_environments/paths/ \
| jq ".results | .[]"
----
. Create a JSON file, for example, `data.json` to define your left and right Ansible lifecycle environments:
+
[source, json, options="nowrap", subs="verbatim,quotes,attributes"]
----
{
  "promote": {
    "source_environment_id": "My_Left_Ansible_Environment_ID_",
    "target_environment_id": "My_Right_Ansible_Environment_ID_"
  }
}
----
. Promote your Ansible lifecycle environment:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--data @data.json \
--header "Accept: application/json" \
--header "Content-Type: application/json" \
--request PATCH \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/lifecycle_environments/paths/_My_Ansible_Lifecycle_Environment_Path_ID_/promote/ \
| jq
----

// TODO: Fix API endpoint
.Verification
* Query the {Project} API to verify that {Project} created the Ansible lifecycle environment successfully:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/lifecycle_environments/ \
| jq ".results | .[] | .name"
----
