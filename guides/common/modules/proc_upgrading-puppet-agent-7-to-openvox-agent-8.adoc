:_mod-docs-content-type: PROCEDURE

[id="upgrade-puppet-agent-7-to-openvox-agent-8"]
= Upgrading Puppet agent 7 to OpenVox agent 8

[role="_abstract"]
If you use Puppet to configure hosts, you can upgrade Puppet agent 7 to OpenVox agent 8.

Puppet agent 7 remains compatible with OpenVox in {Project} as a traditional alternative, but {Team} recommends to upgrade to OpenVox agent 8.
For more information, see {ManagingConfigurationsPuppetDocURL}openvox-versions-supported-for-integration-with-{project-context}[OpenVox versions supported for integration with {Project}] in _{ManagingConfigurationsPuppetDocTitle}_.

.Prerequisites
* You have enabled the *{project-client-name} OpenVox* repository on the host.

.Procedure
* On your hosts, upgrade the `puppet-agent` agent package to `openvox-agent`:
+
[options="nowrap" subs="+quotes,verbatim,attributes"]
----
# {client-package-update-el8} puppet-agent
----
+
You can also use remote execution to upgrade the package on many hosts.
For more information, see {ManagingHostsDocURL}Upgrading_a_package_managing-hosts[Upgrading a package] in _{ManagingHostsDocTitle}_.
