:_mod-docs-content-type: PROCEDURE

[id="Keeping_Track_of_Your_Exports_{context}"]
= Keeping track of your exports

[role="_abstract"]
Track content exports to specific downstream {ProjectServer}s to organize incremental exports and monitor content distribution.
Export tracking enables efficient content management across multiple disconnected environments.

When exporting content from the upstream {ProjectServer} for several downstream {ProjectServer}s, you can also keep track of content exported for specific servers.
This helps you track which content was exported and to where.

.Procedure
. Optional: Track the destinations of content exports.
Use the `--destination-server` argument during export to indicate the target server.
This option is available for all `content-export` operations.
+
For example, specify the destination server when exporting a content view version:
+
[options="nowrap" subs="+quotes"]
----
$ hammer content-export complete version \
--content-view="_My_Content_View_Name_" \
--destination-server=_My_Downstream_Server_1_ \
--organization="_My_Organization_" \
--version=1.0
----
. Query export records by listing the exports:
+
[subs="+quotes"]
----
$ hammer content-export list \
--organization="_My_Organization_"
----
