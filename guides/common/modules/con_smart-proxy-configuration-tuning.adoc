:_mod-docs-content-type: CONCEPT

[id="Smart_Proxy_Configuration_Tuning_{context}"]
= {SmartProxy} configuration tuning

[role="_abstract"]
Understanding {SmartProxy} tuning helps you spread load and deliver content across different networks.
Properly set up {SmartProxies} reduce load on {ProjectServer} and improve performance for remote execution and content delivery.

{SmartProxies} do not improve performance for operations that extensively use the {Project} API,
ifdef::katello,orcharhino,satellite[]
such as host registration or package profile update.
endif::[]
ifdef::foreman-el,foreman-deb[]
such as host registration.
endif::[]
