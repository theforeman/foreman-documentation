:_mod-docs-content-type: PROCEDURE

[id="assign-an-ansible-environment-to-a-host-by-using-cli"]
= Assign an Ansible environment to a host by using Hammer CLI

[role="_abstract"]
You can assign an Ansible environment to your host by using Hammer CLI.
Ansible environments define the lifecycle of the Ansible content that is used to configure your host.

.Prerequisites
// TODO: Fix list of permissions
* Your {Project} account has a role that grants the `create_ansible_lifecycle_environment_paths`, `edit_ansible_lifecycle_environment_paths`, `view_ansible_content`, `view_ansible_lifecycle_environment_paths`, `view_ansible_lifecycle_environments`, and `view_organizations` permissions.
* Your {Project} contains an Ansible lifecycle environment with an Ansible Execution Environment and Ansible content.
* You have installed the Hammer CLI plugin for Ansible Director.
For more information, see xref:install-the-hammer-cli-plugin-for-ansible-director[].

.Procedure
* Assign an Ansible environment to your host:
+
----
$ hammer
----

.Verification
* Verify that {Project} shows your newly assigned Ansible lifecycle environment path and Ansible lifecycle environment:
+
----
$ hammer
----
