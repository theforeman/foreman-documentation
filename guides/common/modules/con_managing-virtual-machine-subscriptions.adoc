= Managing virtual machine subscriptions

You can check for subscriptions that require a `virt-who` configuration.

Then, you can create a `virt-who` configuration file and deploy it on a hypervisor or virtualization manager.

Optionally, you can edit or delete a `virt-who` configuration file.