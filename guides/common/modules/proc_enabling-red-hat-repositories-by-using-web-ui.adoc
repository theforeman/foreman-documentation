:_mod-docs-content-type: PROCEDURE

[id="enabling-red-hat-repositories-by-using-web-ui"]
= Enabling Red{nbsp}Hat repositories by using {ProjectWebUI}

[role="_abstract"]
Enable the Red{nbsp}Hat repositories that match the release and architecture of your hosts so {Project} can synchronize the content from the Red{nbsp}Hat CDN.

To select the repositories to synchronize, you must first identify the product that contains the repository, and then enable that repository based on the relevant release version and base architecture.

.Prerequisites
* If outside network access requires usage of an HTTP proxy, a default HTTP proxy for your {ProjectServer} is configured.
ifndef::orcharhino[]
For more information, see {InstallingServerDocURL}adding-a-default-http-proxy-by-using-web-ui[Adding a default HTTP proxy by using {ProjectWebUI}].
endif::[]
* Your Red{nbsp}Hat subscription manifest is imported to your organization and contains subscriptions.
ifndef::orcharhino[]
For more information, see xref:importing-red-hat-subscription-manifests-into-{project-context}[].
endif::[]

.Procedure
. In the {ProjectWebUI}, navigate to *Content* > *Red{nbsp}Hat Repositories*.
. From the package type list, select the required package types, such as RPM or Kickstart.
. To find repositories, either enter the repository name, or toggle the *Recommended Repositories* button to the on position to view a list of repositories that you require.
. In the *Available Repositories* pane, click a repository to expand the repository set.
. Click the *Enable* icon next to the base architecture and release version that you want.

.Next steps
* xref:synchronizing-content-to-{project-context}[]

.Additional resources
* xref:required-red-hat-repositories[]
