:_mod-docs-content-type: CONCEPT

[id="install-{smart-proxy-context}-server"]
= Install {SmartProxyServer}

[role="_abstract"]
Install {SmartProxyServer} to extend {ProjectServer} services into additional locations, decrease the load on {ProjectServer}, and reduce bandwidth usage across your {Project} deployment.
