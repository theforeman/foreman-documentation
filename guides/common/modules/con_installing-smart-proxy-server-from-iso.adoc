:_mod-docs-content-type: CONCEPT

[id="installing-{smart-proxy-context}-server-from-iso"]
= Installing {SmartProxyServer} from ISO

[role="_abstract"]
To install {SmartProxyServer}, mount the ISO file, run the preparation script, run {foremanctl} to deploy {SmartProxyServer}, and set up SSL certificates for content management and remote execution.
