:_mod-docs-content-type: CONCEPT

[id="installing-{smart-proxy-context}-server-from-iso"]
= Installing {SmartProxyServer} from ISO

[role="_abstract"]
Use ISO installation to deploy {SmartProxyServer} in a disconnected environment.
An ISO image provides the packages required to install {SmartProxyServer} on a host that has no internet access.
