:_mod-docs-content-type: PROCEDURE

[id="delete-imported-ansible-content-by-using-api"]
= Delete imported Ansible content by using {Project} API

[role="_abstract"]
You can delete imported Ansible content from {Project} by using {Project} API.
This procedure deletes all versions of your imported Ansible content from {Project}.

.Prerequisites
* Your {Project} account has a role that grants the `destroy_ansible_content`, `view_ansible_content`, and `view_organizations` permissions.

.Procedure
. Optional: List imported Ansible content:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/ansible_content/ \
| jq ".results | .[]"
----
. Create a JSON file, for example, `data.json` to define the Ansible content that you want to delete:
+
[source, json, options="nowrap", subs="verbatim,quotes,attributes"]
----
{
  "units": [
    {
      "unit_id": "_My_Ansible_Content_ID_"
    }
  ]
}
----
. Delete imported Ansible content from {Project}:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--data @data.json \
--header "Accept: application/json" \
--header "Content-Type: application/json" \
--request DELETE \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/ansible_content/ \
| jq
----

.Verification
* List imported Ansible content on {Project}:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/ansible_content/ \
| jq ".results | .[]"
----
+
Verify that the Ansible content that you have deleted is not part of the output.
