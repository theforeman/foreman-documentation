[id="prerequisites-for-{project-context}-on-aws"]
= Prerequisites for {Project} on AWS

Before you can install {ProjectServer} and register {SmartProxyServers}, you must set up accounts with Amazon Web Services (AWS) and create and start {client-os} instances on AWS.
