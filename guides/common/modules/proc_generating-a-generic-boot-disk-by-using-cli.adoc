:_mod-docs-content-type: PROCEDURE

[id="generating-a-generic-boot-disk-by-using-cli"]
= Generating a generic boot disk by using Hammer CLI

[role="_abstract"]
Generate a boot disk ISO not associated with a specific host that chain loads the installer from {ProjectServer}.

This image is also available from the `/disks/generic` URL path on your {ProjectServer}, for example, `\https://{foreman-example-com}/disks/generic`.

.Prerequisites
* Ensure that the `{ipxe-package}` package is installed on your {ProjectServer}.

.Procedure
* Generate the *Generic image* boot disk:
+
[options="nowrap" subs="+quotes"]
----
$ hammer bootdisk generic
----
