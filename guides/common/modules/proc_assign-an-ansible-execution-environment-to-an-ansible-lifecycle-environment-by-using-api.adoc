:_mod-docs-content-type: PROCEDURE

[id="assign-an-ansible-execution-environment-to-an-ansible-lifecycle-environment-by-using-api"]
= Assign an Ansible Execution Environment to an Ansible lifecycle environment by using {Project} API

[role="_abstract"]
You can assign an Ansible Execution Environment to an Ansible lifecycle environment by using {Project} API.
Ansible Execution Environments provide the environment to run Ansible to configure hosts.

.Prerequisites
* Your {Project} account has a role that grants the `create_ansible_lifecycle_environment_paths`, `edit_ansible_lifecycle_environment_paths`, `view_ansible_content`, `view_ansible_lifecycle_environment_paths`, `view_ansible_lifecycle_environments`, and `view_organizations` permissions.
* Your {Project} contains an Ansible lifecycle environment path.
* Your {Project} contains an Ansible Execution Environment.

.Procedure
. Optional: Get a list of all Ansible Execution Environments:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/execution_environments/ \
| jq ".results | .[]"
----
. Create a JSON file, for example, `data.json` to define your Ansible Execution Environment:
+
[source, json, options="nowrap", subs="verbatim,quotes,attributes"]
----
{
  "lifecycle_environment": {
    "description": "_My_Description_",
    "name": "_My_Ansible_Lifecycle_Environment_Name_",
    "execution_environment_id": "_My_Ansible_Execution_Environment_ID_"
  }
}
----
. Assign an Ansible Execution Environment to your Ansible lifecycle environment:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--data @data.json \
--header "Accept: application/json" \
--header "Content-Type: application/json" \
--request PUT \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/lifecycle_environments/_My_Ansible_Lifecycle_Environment_ID_/ \
| jq
----

.Verification
* Query the {Project} API to verify that {Project} assigned the Ansible Execution Environment to your Ansible lifecycle environment successfully:
+
[options="nowrap", subs="verbatim,quotes,attributes"]
----
$ curl \
--header "Accept: application/json" \
--user "_My_User_Name_:__My_Password__" \
https://{foreman-example-com}/api/v2/ansible_director/lifecycle_environments/_My_Ansible_Lifecycle_Environment_ID_/ \
| jq
----
