:_mod-docs-content-type: PROCEDURE

[id="installing-openvox-server-on-{project-context}-server-and-{smart-proxy-context}-servers"]
= Installing OpenVox server on {ProjectServer} and {SmartProxyServers}

[role="_abstract"]
Use the `{foreman-installer}` utility to install an OpenVox server on {ProjectServer}.
Optionally, you can also install additional OpenVox servers on {SmartProxyServers}.

.Procedure
. Install OpenVox server on {ProjectServer}:
+
[options="nowrap" subs="+quotes,attributes"]
----
# {foreman-installer} \
--enable-foreman-cli-puppet \
--enable-foreman-plugin-puppet \
--enable-puppet \
--foreman-proxy-puppet true \
--foreman-proxy-puppetca true \
--puppet-server true
----
. Optional: If you want manage configuration with Puppet on {SmartProxyServers}, install OpenVox server on {SmartProxyServers}:
+
[options="nowrap" subs="+quotes,attributes"]
----
# {foreman-installer} \
--enable-puppet \
--foreman-proxy-puppet true \
--foreman-proxy-puppetca true \
--puppet-server true
----
