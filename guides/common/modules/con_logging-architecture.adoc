:_mod-docs-content-type: CONCEPT

[id="logging-architecture-overview"]
= Logging architecture overview

[role="_abstract"]
{ProjectServer} routes all service output through `systemd` and `journald`.
You can use `journalctl` commands to view, filter, and manage logs for all {Project} services from a single interface.

All {Project} services log to standard output (`stdout`) by default.
The container runtime captures this output and forwards it to `journald`.

The Apache HTTP Server (`httpd`) uses `systemd-cat` to pipe log output to `journald` with service-specific tags, such as `httpd-error` and `httpd-access`.

ifdef::satellite,katello,orcharhino[]
== Log level configuration support

You can set log levels globally or per service by using `{foremanctl} deploy` parameters.
Not all services support per-service log level parameters.

The following services require their native configuration mechanisms to change log levels:

* Candlepin (Log4j)
* PostgreSQL (`postgresql.conf`)

Valkey uses a different log level vocabulary than other {Project} services.
Instead of the standard `debug`, `info`, `warn`, `error`, and `fatal` levels, Valkey uses `debug`, `verbose`, `notice`, `warning`, and `nothing`.
endif::[]

NOTE: In disconnected environments, the logging architecture works the same way as in connected environments because all logging is local to the host.
