:_mod-docs-content-type: PROCEDURE

[id="uploading-files-to-a-python-type-repository-by-using-cli"]
= Uploading files to a Python type repository by using Hammer CLI

[role="_abstract"]
You can upload files from your local machine to a Python type repository by using Hammer CLI

.Procedure
* Upload files to your Python type repository:
+
[options="nowrap" subs="+quotes"]
----
$ hammer repository upload-content \
--id _My_Repository_ID_ \
--organization-id _My_Organization_ID_ \
--path _Path_To_My_File_
----
+
The `--path` option can indicate a file, a directory of files, or a glob expression of files.
Globs must be escaped by single or double quotes.

.Verification
* Visit the *Published At* URL to see the file.
