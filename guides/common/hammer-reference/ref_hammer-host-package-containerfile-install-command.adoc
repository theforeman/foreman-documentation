:_mod-docs-content-type: REFERENCE

[id="hammer-host-package-containerfile-install-command"]
= host package containerfile-install-command

[role="_abstract"]
Generate a Containerfile RUN command from transiently installed packages on image mode hosts

*Usage*

----
# hammer host package containerfile-install-command [OPTIONS]
----

*Options*

* `--full-result BOOLEAN` {endash} Whether or not to show all results
* `--host VALUE` {endash} Host name
* `--host-id NUMBER` {endash} ID of the host
* `--include-unknown-persistence BOOLEAN` Include packages with unknown persistence in addition to transient packages
* `--order VALUE` {endash} Sort field and order, eg. `id DESC`
* `--page NUMBER` {endash} Page number, starting at 1
* `--per-page NUMBER` {endash} Number of results per page to return
* `--search VALUE` {endash} Search string
* `--sort-by VALUE` {endash} Field to sort the results on
* `--sort-order VALUE` {endash} How to order the sorted results (e.g. ASC for ascending)
* `-h`, `--help` {endash} Print help


