:_mod-docs-content-type: REFERENCE

[id="hammer-flatpak-remote-remote-repository-list"]
= flatpak-remote remote-repository list

[role="_abstract"]
List flatpak remote repositories

*Usage*

----
# hammer flatpak-remote remote-repository <list|index> [OPTIONS]
----

*Options*

* `--fields LIST` {endash} Show specified fields or predefined field sets only. (See below)
* `--flatpak-remote VALUE` {endash} Flatpak remote name to search by
* `--flatpak-remote-id NUMBER` {endash} ID of flatpak remote to show repositories of
* `--full-result BOOLEAN` {endash} Whether or not to show all results
* `--label VALUE` {endash} Label of the flatpak remote
* `--name VALUE` {endash} Name of the flatpak remote repository
* `--order VALUE` {endash} Sort field and order, eg. `id DESC`
* `--organization VALUE` {endash} Organization name to search by
* `--organization-id NUMBER` {endash} Organization identifier
* `--organization-label VALUE` {endash} Organization label to search by
* `--organization-title VALUE` {endash} Organization title
* `--page NUMBER` {endash} Page number, starting at 1
* `--per-page NUMBER` {endash} Number of results per page to return
* `--search VALUE` {endash} Search string
* `-h`, `--help` {endash} Print help

*Predefined field sets*

|===
| FIELDS | ALL | DEFAULT | THIN

| Id     | x   | x       | x
| Name   | x   | x       | x
| Label  | x   | x       |
|===

*Search and order fields*

* `flatpak_remote_id` {endash} integer
* `id` {endash} integer
* `label` {endash} string
* `name` {endash} string

:_mod-docs-content-type: REFERENCE

[id="hammer-flatpak-remote-remote-repository-list"]
= flatpak-remote remote-repository list

[role="_abstract"]
List flatpak remote repositories

*Usage*

----
# hammer flatpak-remote remote-repository <list|index> [OPTIONS]
----

*Options*

* `--fields LIST` {endash} Show specified fields or predefined field sets only. (See below)
* `--flatpak-remote VALUE` {endash} Flatpak remote name to search by
* `--flatpak-remote-id NUMBER` {endash} ID of flatpak remote to show repositories of
* `--full-result BOOLEAN` {endash} Whether or not to show all results
* `--label VALUE` {endash} Label of the flatpak remote
* `--name VALUE` {endash} Name of the flatpak remote repository
* `--order VALUE` {endash} Sort field and order, eg. `id DESC`
* `--organization VALUE` {endash} Organization name to search by
* `--organization-id NUMBER` {endash} Organization identifier
* `--organization-label VALUE` {endash} Organization label to search by
* `--organization-title VALUE` {endash} Organization title
* `--page NUMBER` {endash} Page number, starting at 1
* `--per-page NUMBER` {endash} Number of results per page to return
* `--search VALUE` {endash} Search string
* `-h`, `--help` {endash} Print help

*Predefined field sets*

|===
| FIELDS | ALL | DEFAULT | THIN

| Id     | x   | x       | x
| Name   | x   | x       | x
| Label  | x   | x       |
|===

*Search and order fields*

* `flatpak_remote_id` {endash} integer
* `id` {endash} integer
* `label` {endash} string
* `name` {endash} string

