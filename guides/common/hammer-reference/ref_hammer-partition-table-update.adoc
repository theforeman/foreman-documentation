:_mod-docs-content-type: REFERENCE

[id="hammer-partition-table-update"]
= partition-table update

[role="_abstract"]
Update a partition table

*Usage*

----
# hammer partition-table update [OPTIONS]
----

*Options*

* `--audit-comment VALUE`
* `--description VALUE`
* `--file FILE` {endash} Path to a file that contains the partition layout
* `--host-ids LIST` {endash} Array of host IDs to associate with the partition table
* `--hostgroup-ids LIST` {endash} Array of host group IDs to associate with the partition table
* `--hostgroup-titles LIST`
* `--hostgroups LIST`
* `--hosts LIST`
* `--id VALUE`
* `--location VALUE` {endash} Set the current location context for the request
* `--location-id NUMBER` {endash} Set the current location context for the request
* `--location-ids LIST` {endash} REPLACE locations with given ids
* `--location-title VALUE` {endash} Set the current location context for the request
* `--location-titles LIST`
* `--locations LIST`
* `--locked BOOLEAN` {endash} Whether or not the template is locked for editing
* `--name VALUE`
* `--new-name VALUE`
* `--operatingsystem-ids LIST` {endash} Array of operating system IDs to associate with the partition table
* `--operatingsystems LIST`
* `--organization VALUE` {endash} Set the current organization context for the request
* `--organization-id NUMBER` {endash} Set the current organization context for the request
* `--organization-ids LIST` {endash} REPLACE organizations with given ids.
* `--organization-title VALUE` {endash} Set the current organization context for the request
* `--organization-titles LIST`
* `--organizations LIST`
* `--os-family VALUE`
* `--snippet BOOLEAN`
* `-h`, `--help` {endash} Print help


:_mod-docs-content-type: REFERENCE

[id="hammer-partition-table-update"]
= partition-table update

[role="_abstract"]
Update a partition table

*Usage*

----
# hammer partition-table update [OPTIONS]
----

*Options*

* `--audit-comment VALUE`
* `--description VALUE`
* `--file FILE` {endash} Path to a file that contains the partition layout
* `--host-ids LIST` {endash} Array of host IDs to associate with the partition table
* `--hostgroup-ids LIST` {endash} Array of host group IDs to associate with the partition table
* `--hostgroup-titles LIST`
* `--hostgroups LIST`
* `--hosts LIST`
* `--id VALUE`
* `--location VALUE` {endash} Set the current location context for the request
* `--location-id NUMBER` {endash} Set the current location context for the request
* `--location-ids LIST` {endash} REPLACE locations with given ids
* `--location-title VALUE` {endash} Set the current location context for the request
* `--location-titles LIST`
* `--locations LIST`
* `--locked BOOLEAN` {endash} Whether or not the template is locked for editing
* `--name VALUE`
* `--new-name VALUE`
* `--operatingsystem-ids LIST` {endash} Array of operating system IDs to associate with the partition table
* `--operatingsystems LIST`
* `--organization VALUE` {endash} Set the current organization context for the request
* `--organization-id NUMBER` {endash} Set the current organization context for the request
* `--organization-ids LIST` {endash} REPLACE organizations with given ids.
* `--organization-title VALUE` {endash} Set the current organization context for the request
* `--organization-titles LIST`
* `--organizations LIST`
* `--os-family VALUE`
* `--snippet BOOLEAN`
* `-h`, `--help` {endash} Print help


